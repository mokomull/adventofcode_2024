use prelude::Day;

use crate::Solution;

static INPUT: &str = "";
#[test]
fn day_05() {
    let solution = Solution::new(INPUT);
    assert_eq!(0, solution.part1().unwrap());
    assert_eq!(0, solution.part2().unwrap());
}
